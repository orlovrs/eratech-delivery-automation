{
  "name": "google-api-tests",
  "version": "1.0.0",
  "scripts": {
    "test": "cucumber-js",
    "test:ci": "cucumber-js --format json:reports/cucumber_report.json & node report.js",
    "test:ci:only": "cucumber-js --format json:reports/cucumber_report.json --tags \"@only\" & node report.js"
  },
  "dependencies": {
    "axios": "^1.12.1",
    "chai": "^6.0.1",
    "cucumber": "^6.0.7",
    "cucumber-html-reporter": "^7.2.0"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^12.2.0"
  }
}
